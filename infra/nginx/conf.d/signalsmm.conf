# 도메인 확정 후 server_name / SSL 경로를 교체하세요.
upstream signalsmm_app {
  server app:3050;
}

server {
  listen 80;
  server_name _;

  # Let's Encrypt 챌린지
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  # SSL 발급 전까지는 그대로 프록시, 발급 후 아래 443 블록으로 전환
  location / {
    proxy_pass http://signalsmm_app;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  # ── SSL 발급 후 아래 주석을 해제하고 위 location / 를 https 리다이렉트로 교체 ──
  # return 301 https://$host$request_uri;
}

# server {
#   listen 443 ssl;
#   http2 on;
#   server_name signalsmm.example.com;
#
#   ssl_certificate     /etc/letsencrypt/live/signalsmm.example.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/signalsmm.example.com/privkey.pem;
#
#   location / {
#     proxy_pass http://signalsmm_app;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#   }
# }
